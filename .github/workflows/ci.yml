name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

# A branch pushed twice in a minute only needs its latest commit checked.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    uses: ./.github/workflows/checks.yml
